Ты гуру разработки на Python, ты  всегда пишешь надежный, безопасный, производительный код, при этом код легко читается и поддерживается начинающими разработчиками, все комментарии на русском языке. В сложных для начинающих разработчиков местах есть развертнутые пояснения. Созадй агент: Генератор SEO-заголовков для блога. Строго следуй ТЗ:
🎯 Цель

Создать агента, который на основе статьи по URL генерирует 5 цепляющих SEO-заголовков.



⚙️ Логика работы

1. Загружает HTML по ссылке, извлекает текст с помощью BeautifulSoup.
2. Создаёт системный промпт: «Ты опытный порффесиональный SEO-редактор. Придумай 5 заголовков»
3. Отправляет текст статьи в LLM с этим промптом.
4. Возвращает массив из 5 заголовков.


✅ Функциональные требования
- Агент вызывается функцией с аргументом url.
- Используется модель OpenAI (gpt-4o или gpt-3.5-turbo).
- Промпт передаётся как system, текст статьи — как user.
- Результат — список из 5 заголовков.
- Повторы при сбоях до 3 раз.

🛡 Нефункциональные требования
- логирование ошибок с  контекстом  loguru 
- PEP8 + типизация (mypy).
- Зависимости: openai, requests, beautifulsoup4, dotenv, tenacity.
- Python ≥ 3.10.
- Ключ OpenAI хранится в .env  в переменной PROXYAPIKEY
- Используемая модель в .env  в переменной MODEL
- URL для обращения к модели в в .env переменной OPENAPIURL

📁 Структура проекта
project/
├── agent.py
├── openai_module.py
├── requirements.txt
└── README.md

🧪 Приёмка
- Команда python agent.py  возвращает список заголовков.
- Ошибки обрабатываются корректно.
- Результат логично соотносится с содержанием статьи.
